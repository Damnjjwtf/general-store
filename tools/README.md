# tools/

Project-local scripts only, if any are ever needed (e.g. a Fountain → PDF build for `scripts/`).

The general workflow / dev layer for this project comes from two standard Claude Code repos we reuse across projects, installed globally (not vendored here):

- **ponytail** — https://github.com/DietrichGebert/ponytail
- **gstack** — https://github.com/garrytan/gstack

See the repo root `README.md` for how they're set up.
