# tools/

The real toolchain lives in the **gstack** repo (`continuity-lint`, `script-build`, `structure-import`) so it stays reusable across projects. This folder holds only project-local glue if any is ever needed.

For now: nothing to run here. See `AGENTS.md` §2–4 and the gstack README.
